import fs from 'fs'; import { createClient } from '@supabase/supabase-js'; const env = fs.readFileSync('.env', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1].trim(); const supabase = createClient(url, key); async function test() { const { data: r } = await supabase.from('billetera_recargas').select('id').eq('estado', 'pendiente').limit(1); if(r && r.length > 0) { const { data, error } = await supabase.rpc('procesar_recarga_automatica_rpc', { p_recarga_id: r[0].id }); console.log(data, error); } else { console.log('No pending recharges'); } } test();
