import fs from 'fs'; import { createClient } from '@supabase/supabase-js'; const env = fs.readFileSync('.env', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const key = env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/)?.[1]?.trim() || env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1].trim(); const supabase = createClient(url, key); async function run() { const { data, error } = await supabase.from('pagos_apk').select(*, pedidos ( numero_pedido, total_bs, total_usd ), perfiles!usuario_id ( nombres, apellidos, usuario )).limit(1); console.log('Data:', data, 'Error:', error); } run();
