import fs from 'fs'; import { createClient } from '@supabase/supabase-js'; const env = fs.readFileSync('.env', 'utf-8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1].trim(); const supabase = createClient(url, key); async function run() { const {data: admin} = await supabase.from('perfiles').select('id').eq('rol', 'admin').limit(1).single(); console.log('Admin ID:', admin?.id); const { data, error } = await supabase.rpc('aprobar_recarga_rpc', { p_recarga_id: '00000000-0000-0000-0000-000000000000', p_admin_id: admin?.id || '00000000-0000-0000-0000-000000000000' }); console.log(data, error); } run();
